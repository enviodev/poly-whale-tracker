{
  "name": "poly-whales",
  "version": "0.0.3",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node --external blessed",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@envio-dev/hypersync-client": "^1.1.0",
    "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "bin": {
    "poly-whales": "./dist/index.js"
  }
}
